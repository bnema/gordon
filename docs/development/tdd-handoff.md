# TDD handoff

Record the failing test name and command written before implementation, the smallest implementation commit, the passing command, and any required release/security gate from the [docs ownership matrix](../reference/all-container-refactor-docs-map.md).

Security acceptance is complete only when its selected scenario is executable and blocking; pending scenarios receive no coverage credit. Redact all credentials, component environments, socket paths, and raw artifacts.
