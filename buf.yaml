version: v2

modules:
  - path: api/proto
    name: buf.build/bnema/gordon

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - FILE
