#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(CDPATH= cd -- "$script_dir/../.." && pwd)
export GORDON_REPO_ROOT="$repo_root"
cd "$repo_root"
exec go run ./dev/v3/cmd/sandbox "$@"
